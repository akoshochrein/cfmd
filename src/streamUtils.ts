
// TODO, research how we can use piping and command line args together

// import * as readline from 'readline';

// const rl = readline.createInterface({
//     input: process.stdin,
//     output: process.stdout,
//     terminal: false,
// });

// rl.on("line", (line: string) => {
//     input += `${line}\n`;
// })

// rl.on('close', () => {
// })
